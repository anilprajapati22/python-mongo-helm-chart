apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  labels:
    io.kompose.service: nodejs-kubernetes-mongodb-volume
  name: nodejs-kubernetes-mongodb-volume
  namespace: {{ .Values.nameSpace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi

